// Used for QUnit testing only.  If you want to run the tests, copy this file
// to a new file called `simplenote-credentials.js` and adjust your
// credentials.

window.CREDENTIALS = {
  email: "you@example.com",
  password: "myPassword"
};